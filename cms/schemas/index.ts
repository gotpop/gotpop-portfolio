import blockContent from './blockContent'
import category from './category'
import project from './project'
import siteConfig from './siteConfig'

export const schemaTypes = [category, project, blockContent, siteConfig]
